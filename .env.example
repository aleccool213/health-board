# Protects /api/cron/remind. Vercel cron sends Authorization: Bearer $CRON_SECRET
CRON_SECRET=

# Same Marketplace Redis names as Split Log (Vercel → Storage → Redis).
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Same VAPID pair as Split Log so you can copy the private key from that project.
VAPID_PUBLIC_KEY=BHLuym4du_T_Sdvx-3G_UcCMzf_m3Ny0NzEkllGvMlaxCWHTiWCIeLidDkCRcL2T5cNvZr4ZukQvltUJSctssaQ
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:alec@alec.coffee
